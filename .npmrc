# Nuxt's module ecosystem carries a lot of optional and loose peer ranges.
strict-peer-dependencies=false

# Keep the strict node_modules layout. Nuxt 4 resolves fine without hoisting,
# and a flat tree would hide missing dependencies until they break in Docker.
shamefully-hoist=false
